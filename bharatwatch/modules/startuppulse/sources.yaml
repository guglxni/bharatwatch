module: startuppulse
name: Startuppulse
sources:
  - name: startup-local
    url: https://alexandria-circles-chassis-hub.trycloudflare.com/startup/
    collector_id: c_mt1hcxap876dyo54k
    status: healthy
    prompt: |
      Extract all rows from the schemes table. For each row, return: title, ministry, scheme_type, deadline, summary, and link. Do not navigate any links. Return a flat JSON array.
